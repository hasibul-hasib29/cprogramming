#include<stdio.h>
#include<string.h>
int main(){
    char str[7];
    int a;
    scanf("%d", &a);
    fgetc(stdin);

    fgets(str, 7, stdin);

    int i=0;

    

    // for(int i=0; i<7; i++)
    //   printf("<%c>\n", str[i]);




/*
    int a;
    printf("enter the length : ");
    scanf("%d", &a);
    fgetc(stdin);

    char str[a+1];
    fgets(str, a+1, stdin);

    for(int i=0; i<a ; i++)
     printf("<%c>\n",str[i] );


     puts(str);


     printf("\n");

     char ab[a+1];
    char temp;
     for(int i=0; i<a; i++)
        for(int j=i+1; j<a; j++){
            if(str[i]>str[j])
             
             temp = str[i];
             str[i]= str[j];
             str[j] = temp ;
        }

        puts(str);
    return 0;

    */

   // some changes

   // hey you
}